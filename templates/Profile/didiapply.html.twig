{% include 'RawTemplate/_hearder.html.twig' %}

<div id="mg-graph-identifier">pro-didia</div>

<div class="row">
  <div class="col-lg-3 mgs-mainblk-txt">
  </div>

  <div class="col-lg-6">
    <div class="mgs-questiondefinition">
      <form action="{{ path('verify_application') }}" method="post" class="mg-verify-page" autocomplete="new-password">
          <div>
            <div class="mg-verify-logo-250">
            <img src="{{ path('main') }}img/check500_500px.png" style="width:200px; height:200px; opacity: 0.8;"
                      title="MGScoreCertificate" alt="MGScore Certification">

            </div>
            <div class="middle-txt">
              <span class="icon-search nav-icon-fa-sm nav-text"></span>&nbsp;Vérifier ici si vous êtes bien inscrit pour obtenir votre carte d'étudiant avec code barre.<br>
              <strong>Attention : il ne s'agit pas de l'inscription au concours</strong>
              <br>Renseignez les informations suivantes :
            </div>
            <div class="row">
              <div class="col-lg-5 form-group">

                {# <textarea class="form-control" rows="1" id="inputMG" name="inputUserName" placeholder="Username"></textarea> #}
                <input class="form-control" rows="1" id="inputLN" name="inputLastName" placeholder="Nom de famille">
              </div>
              <div class="col-lg-4 form-group">
                {# <textarea class="form-control" rows="1" id="inputMGCode" name="inputPassword" placeholder="Mot de passe"></textarea> #}
                <input class="form-control" rows="1" id="inputBirthD" name="inputBirthday" placeholder="Date de naissance JJ-MM-AAAA">
              </div>
              <div class="col-lg-3 form-group mg-verify-bar">
                <input type="submit" name="formSubmit" id="btnCheckDidApp" class="btn btn btn-outline-dark btn-md btn-block" value="Vérifier"/>
              </div>
              <div id="help-mess" class="col-lg-12 alert alert-danger" role="alert">
                Le nom de famille ne peut être vide et votre date de naissance doit être au format JJ-MM-AAAA, exemple 17-03-1997
              </div>
            </div>
          </div>

      </form>
    </div>
  </div>


  <div class="col-lg-3 mgs-mainblk-txt">
  </div>
</div>

{% include 'RawTemplate/_footer.html.twig' %}
